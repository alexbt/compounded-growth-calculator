<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/navigation_marital_status">

    <fragment
        android:id="@+id/navigation_marital_status"
        android:name="com.alexbt.finance.growth.InputFragment"
        android:label="@string/assets"
        tools:layout="@layout/fragment_liabilities" />

    <fragment
        android:id="@+id/navigation_infos"
        android:name="com.alexbt.finance.growth.LiabilitiesFragment"
        android:label="@string/liabilities"
        tools:layout="@layout/fragment_inputs" />

    <fragment
        android:id="@+id/navigation_results"
        android:name="com.alexbt.finance.growth.ResultFragment"
        android:label="@string/results"
        tools:layout="@layout/fragment_result" />

    <fragment
        android:id="@+id/navigation_settings"
        android:name="com.alexbt.finance.growth.settings.SettingsFragment"
        android:label="@string/settings"
        tools:layout="@layout/fragment_settings" />

</navigation>